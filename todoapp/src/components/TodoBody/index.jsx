import TodoNavBar from "../TodoNavBar";
import TodoList from "../TodoList";

const TodoBody = () => {
    return (
        <>
            <TodoNavBar />
            <TodoList />
        </>
    );
};

export default TodoBody;
